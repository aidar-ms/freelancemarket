

/*export default {
    data() {
        return {
            contracts: [],
            errors: []
        }
    },

    created() {
        axios.get('/contracts')
            .then(response => {
                console.log(response.data);
                this.contracts = response.data;
            })
            .catch(e => {
                this.errors.push(e)
            })
    }

} */